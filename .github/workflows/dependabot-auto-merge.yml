# Auto-merge Dependabot PRs once CI is green. Patch + minor bumps only —
# major bumps stay manual. Squash + delete-branch keeps main linear.
#
# Without this, Dependabot opens PRs that nobody merges and the repo drifts
# while looking maintained.

name: Dependabot auto-merge

on: pull_request_target

permissions:
  contents: write
  pull-requests: write

jobs:
  auto-merge:
    runs-on: ubuntu-latest
    if: ${{ github.actor == 'dependabot[bot]' }}
    steps:
      - name: Fetch Dependabot metadata
        id: meta
        uses: dependabot/fetch-metadata@v3

      - name: Queue auto-merge for patch + minor bumps
        if: ${{ steps.meta.outputs.update-type == 'version-update:semver-patch'
             || steps.meta.outputs.update-type == 'version-update:semver-minor' }}
        run: gh pr merge --auto --squash --delete-branch "$PR_URL"
        env:
          PR_URL: ${{ github.event.pull_request.html_url }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}